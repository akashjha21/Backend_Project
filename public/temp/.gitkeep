Since we can't push new folders inside github until and unless we don't have new files inside it. And while making a big project we must keep tht track of each new files as well as folders so we create this .gitkeep file to push that empty folders inside Github.